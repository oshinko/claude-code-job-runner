services:
  runner:
    volumes:
      - type: bind
        # Replace this value in compose.override.yaml with the host project directory.
        source: /path/to/project
        target: ${PROJECT_LOCATION:?Set PROJECT_LOCATION to the absolute container mount path}
